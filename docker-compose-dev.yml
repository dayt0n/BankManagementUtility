version: '3.9'

services:
  bmu-database-dev:
    restart: always
    build:
      context: "./database"
      dockerfile: "./Dockerfile"
    ports: 
      - "5432:5432"
  bmu-api-dev:
    restart: always
    build:
      context: "./api"
      dockerfile: "./Dockerfile"
  bmu-frontend-dev:
    restart: always
    build:
      context: "./frontend"
      dockerfile: "./DockerfileDev"
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./frontend:/app
      - '/app/node_modules'
  bmu-main-dev:
    restart: always
    build:
      context: "./nginx"
      dockerfile: "./DockerfileDev"
    ports: 
      - "80:80"